# Copy this file to .env and fill in your key:
#   cp .env.example .env
#
# Get an OpenAI API key at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional model overrides (defaults live in app/llm.py):
# SQL_MODEL=gpt-4o-mini
# SUMMARY_MODEL=gpt-4o-mini

# Optional: RAG settings (defaults + full list in app/rag/config.py).
# RAG_ENABLED=true
# RAG_TOP_K=3
# RAG_MIN_SCORE=0.10
# RAG_DEBUG=false
# EMBEDDING_MODEL=text-embedding-3-small
